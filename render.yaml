services:
  - type: web
    name: campus-research-backend
    env: node
    rootDir: BackEnd
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
